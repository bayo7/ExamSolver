# VisionSolve MCQ — Python bağımlılıkları
# Kurulum: pip install -r requirements.txt

# Temel
python-docx==1.1.2        # .docx okuma ve yazma
Pillow==10.4.0             # Görsel işleme (crop, stitch, label)
lxml==5.2.2                # XML parsing (docx iç yapısı)
python-dotenv==1.0.1       # .env dosyasından API key okuma

# LLM API clientları (kullandığına göre yükle)
anthropic==0.30.0          # Claude API
openai==1.35.0             # GPT-4o API
google-generativeai==0.7.0 # Gemini API

# Opsiyonel: OCR (taranmış dosyalar için)
pytesseract==0.3.13        # Tesseract OCR wrapper
# Not: pytesseract için sistem paketi de gerekir:
#   Ubuntu: sudo apt install tesseract-ocr
#   Mac:    brew install tesseract
